(()=>{
function detailInfo(){const common=document.getElementById('detailCommonName')?.textContent?.trim()||'Species',scientific=(document.getElementById('detailScientificName')?.textContent||'').replace(/[()]/g,'').trim(),meta=document.getElementById('detailMeta')?.textContent?.trim()||'',family=document.querySelector('#detailDialog .acquired-family-link,#detailDialog .family-link,#detailDialog [data-family-link]')?.textContent?.trim()||'';return{common,scientific,rarity:meta.split('•')[0]?.trim()||'',family}}
function init(){document.addEventListener('click',e=>{const img=e.target.closest?.('.sighting-photos img');if(!img)return;e.preventDefault();e.stopPropagation();const sighting=img.closest('.sighting-card'),date=sighting?.querySelector('.sighting-date')?.textContent?.trim()||'',location=sighting?.querySelector('.sighting-location')?.textContent?.trim()||'',note=sighting?.querySelector('.sighting-notes')?.textContent?.trim()||'',info=detailInfo(),src=img.currentSrc||img.src;const detail=document.getElementById('detailDialog');if(detail?.open)detail.close();window.CompendiumFullscreenPhoto?.open?.({...info,src,date,location:location==='No location'?'':location,note,source:'Your sighting photo'})})}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();