(()=>{
const SEARCH='https://media.ebird.org/catalog?mediaType=audio&birdOnly=true&searchField=species&q=';
function css(){if(document.getElementById('birdSoundStyles'))return;const s=document.createElement('style');s.id='birdSoundStyles';s.textContent=`.bird-sound-btn{position:absolute;right:8px;top:8px;z-index:4;width:30px;height:30px;border-radius:999px;border:1px solid rgba(255,255,255,.32);background:rgba(12,14,16,.72);color:#fff;display:grid;place-items:center;font-size:14px;padding:0;backdrop-filter:blur(5px);-webkit-backdrop-filter:blur(5px);box-shadow:0 2px 8px rgba(0,0,0,.22)}.bird-sound-btn:active{transform:scale(.94)}.bird-sound-detail{position:absolute;right:52px;top:12px;z-index:6;width:34px;height:34px;border-radius:999px;border:1px solid var(--line);background:var(--panel);color:var(--text);display:grid;place-items:center;font-size:16px}.bird-sound-note{font-size:10px;color:var(--muted);margin-left:5px}`;document.head.appendChild(s)}
function speciesFromTile(tile){return tile.querySelector('.tile-name')?.textContent?.trim()||''}
function openSound(name){if(!name)return;const url=SEARCH+encodeURIComponent(name);window.open(url,'_blank','noopener,noreferrer')}
function addTileButtons(){document.querySelectorAll('.bird-tile').forEach(tile=>{if(tile.querySelector('.bird-sound-btn'))return;const name=speciesFromTile(tile);if(!name)return;const b=document.createElement('button');b.type='button';b.className='bird-sound-btn';b.textContent='🔊';b.title=`Listen to ${name}`;b.setAttribute('aria-label',`Listen to ${name}`);b.onclick=e=>{e.preventDefault();e.stopPropagation();openSound(speciesFromTile(tile)||name)};tile.appendChild(b)})}
function addDetailButton(){const d=document.getElementById('detailDialog'),top=d?.querySelector('.detail-top');if(!d||!top||top.querySelector('.bird-sound-detail'))return;const b=document.createElement('button');b.type='button';b.className='bird-sound-detail';b.textContent='🔊';b.title='Listen to this species';b.setAttribute('aria-label','Listen to this species');b.onclick=e=>{e.preventDefault();e.stopPropagation();openSound(document.getElementById('detailCommonName')?.textContent?.trim())};top.appendChild(b)}
let t;function refresh(){clearTimeout(t);t=setTimeout(()=>{addTileButtons();addDetailButton()},80)}function init(){css();refresh();new MutationObserver(refresh).observe(document.getElementById('birdGrid')||document.body,{childList:true,subtree:true});addEventListener('bird-local-change',refresh);document.addEventListener('click',e=>{if(e.target.closest?.('.comp-nav-btn,.ach-tab'))setTimeout(refresh,150)})}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',init):init();
})();